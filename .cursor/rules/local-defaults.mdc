# Cursor CLI Local Defaults

- git.autocommit: false
- package_manager.default: yarn
- package_manager.prevent_npm: true

## Guidelines

- Never auto-commit or push without explicit user request
- Prefer `yarn` commands over `npm`
- For installs: use `yarn add`, `yarn add -D`, `yarn remove`
- Do not create task summary documents unless explicitly requested
